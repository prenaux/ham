#!/bin/bash

#------------------------------------------------------------------
# The below is if we really want to access hoogle from the path
#------------------------------------------------------------------

# export HAM_NO_VER_CHECK=1
# if [[ -z "$HAM_HOME" ]]; then echo "E/HAM_HOME not set !"; exit 1; fi
# . "$HAM_HOME/bin/ham-toolset" haskell > /dev/null

#
# Just access hoogle from the user folder directly, will break on Windows?
#
$HOME/.cabal/bin/hoogle "$@"
