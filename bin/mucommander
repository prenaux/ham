#!/bin/bash
export HAM_NO_VER_CHECK=1
if [[ -z "$HAM_HOME" ]]; then echo "E/HAM_HOME not set !"; exit 1; fi

. "$HAM_HOME/bin/ham-toolset" mucommander > /dev/null

set -ex

java -Djava.library.path="$MUCOMMANDER_JARS_DIR" -cp "$MUCOMMANDER_JARS_MAIN_JAR" com.mucommander.main.muCommander
