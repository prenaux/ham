#!/bin/bash
. ham-toolset perl > /dev/null
args=()
for PARAM in "$@"
do
    if [ ${PARAM:0:1} = "-" ]; then
        args+=(${PARAM})
    else
        args+=("${PARAM}")
    fi
done
perl "$HAM_HOME/bin/ack.pl" "${args[@]}"
