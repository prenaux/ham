#!/bin/bash -e
#
# usage:
#   ham-cppm-build TOOLSETS
#
# example:
#   ham-cppm-build thrift_cppm
#
for toolset in "$@"
do
  ham -D "${HAM_HOME}/specs/toolsets/${toolset}" -T "${HAM_HOME}/specs/toolsets/${toolset}/_ham_project" all
done
