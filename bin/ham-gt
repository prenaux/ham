#!/bin/bash
. "$HAM_HOME/bin/ham-bash-setenv.sh"
export HAM_NO_VER_CHECK=1
toolset_import_once nodejs > /dev/null
if [ "$1" == "update_cli" ]; then
  (set -x ;
   pushd "$HAM_HOME"
   npm install -g @withgraphite/graphite-cli@latest
   popd)
  echo "I/Graphite CLI updated."
  exit 0
fi
gt "$@"
