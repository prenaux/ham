#----------------------------------------------------------------------
#     Toolkit definition
#----------------------------------------------------------------------
TK_NAME = "ham" ;
TK_DIR = [ FGetAbsolutePath [ FDirName $(TOP) .. ] ] ;
TOP_DIR = [ FDirName $(TK_DIR) specs ] ;
TK_NONCE = 1 ;

Import toolkit.ham ;
