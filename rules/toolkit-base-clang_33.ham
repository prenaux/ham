#----------------------------------------------------------------------
#     LOA
#----------------------------------------------------------------------
LOA_OS = $(HAM_OS:L) ;
LOA_LINKER = gcc ;
LOA_ARCH = $(OSPLAT:L) ;

#----------------------------------------------------------------------
#     DEFINES
#----------------------------------------------------------------------
DEFINES += $(HAM_OS) _COMPILER_$(LOA_OS:U)_$(LOA_ARCH:U) ;

#----------------------------------------------------------------------
#     OS Variables
#----------------------------------------------------------------------
POSIX = 1 ;
if $(OS) = MACOSX {
  OSX = 1 ;
}
