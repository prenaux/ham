Import log.ham ;

rule AssertEq {
  if $(<) != $(>) {
    logFatal "ASSERT:" $(<) "!=" $(>) ;
  }
}

rule AssertVarDefined {
  if ! $($(<)) {
    logFatal "ASSERT:" $(<) "variable not defined !" ;
  }
}
